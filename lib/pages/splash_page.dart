import 'package:flutter/material.dart';

class SplashPage extends StatelessWidget {
	SplashPage({Key key}) : super(key: key);

	@override build(context) {
		return Scaffold(
			body: Center(
				child: Text('در حال بارگذاریه محتوا'),
			),
		);
	}
}
